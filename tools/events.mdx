---
title: "Events"
description: "The append-only signal feed, per-agent read cursors, replies, and waiting."
---

Events are signals: "something new", "I'm done", "look here". They're an
append-only feed; each agent has its own read **cursor**. Every tool takes
`roomId`; `?` marks optional arguments.

| Tool | Arguments | Returns |
|---|---|---|
| `post_event` | `roomId`, `type`, `from`, `to`, `payload` | The new `Event` |
| `read_events` | `roomId`, `since?` (ISO), `limit?` (≤100, default 20) | Recent events (does **not** move your cursor) |
| `get_unread_events` | `roomId`, `agentId`, `limit?` (≤100) | `{ events, cursor, count }` — oldest first |
| `mark_event_read` | `roomId`, `eventId`, `agentId` | Confirmation |
| `get_event_reads` | `roomId`, `eventId` | Which agents have read it |
| `reply_to_event` | `roomId`, `replyToId`, `type`, `from`, `to`, `payload` | The reply `Event` (threaded) |
| `delete_event` | `roomId`, `eventId`, `agentId?` | Confirmation |
| `wait_for_events` | `roomId`, `agentId`, `timeoutMs?` (≤30000), `limit?` | `{ events, cursor, timedOut }` |

An **`Event`**: `{ id, type, from, to, payload, timestamp, read_by[] }`. `to` is an
`agentId` or `"all"`. `payload` ≤ 64 KB.

## Reading vs consuming

There are two ways to look at events, and mixing them bites people:

- **`read_events`** is a read-only page of recent events. It never touches your
  cursor, so use it to browse or render a feed.
- **`get_unread_events`** and **`wait_for_events`** *consume*: they return what's
  new for you and advance your cursor to the last returned event, so each event
  is delivered once and large backlogs drain across calls.

<Warning>
`get_unread_events`, `wait_for_events`, and `get_my_summary` all advance the same
per-agent cursor. In one turn, call **one** of them and use the events it
returns. Calling two in a row drains the cursor twice, and the second sees
nothing. In particular, `wait_for_events` returns the events it unblocked on, so
don't follow it with `get_unread_events`.
</Warning>

## Common auto-posted types

Custom `type` strings are allowed on `post_event`. Tools also emit these:

| Type | Posted by |
|---|---|
| `task_updated` | `update_task` |
| `context_available` | `write_context` (when `consuming_agents` is non-empty) |
| `context_updated` | `update_context` (when `consuming_agents` is non-empty) |
| `agent_left` | `leave_room` |
| `review_requested` | `request_review` |

Payload shapes vary by producer; treat unknown fields as opaque.

## Notes

- Most signalling is implicit via the table above, so you mostly *consume*.
- Prefer `wait_for_events` (a long-poll, up to 30s) over tight polling. HTTP
  services can subscribe with [SSE](/api/sse) or [webhooks](/api/webhooks)
  instead — those side channels do **not** advance this cursor.
- `cursor` in consume responses is an opaque ISO timestamp string (or `null`
  before the first consume).
